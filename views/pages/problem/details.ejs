<!DOCTYPE html>
<html>
  <head>
    <title><%= question.title %> - Coding Questions</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f2f2f2;
      }
      h1 {
        color: #4a4a4a;
        text-align: center;
        margin-top: 50px;
      }
      p {
        font-size: 18px;
        color: #4a4a4a;
        text-align: center;
        margin-bottom: 50px;
      }
      .question {
        width: 80%;
        margin: 0 auto;
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.15);
        background-color: #fff;
      }
      .question h3 {
        font-size: 24px;
        color: #4a4a4a;
        margin-top: 0;
        margin-bottom: 10px;
      }
      .question p {
        font-size: 18px;
        color: #4a4a4a;
        margin-top: 0;
      }
      button {
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        background-color: #007bff;
        color: #fff;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s ease;
      }
      button:hover {
        background-color: #0056b3;
      }
    </style>
  </head>
  <body>
    <h1><%= question.title %></h1>
    <div class="question">
      <h3><%= question.title %></h3>
      <p><%= question.description %></p>
      <button onclick="history.back()">Go Back</button>
    </div>
  </body>
</html>