<!DOCTYPE html>
<html>
<head>
	<%- include('../partials/head'); %>
	<link href="../../css/mdb.min.css" rel="stylesheet">
	<title>Text Message Box</title>
	<style>
	
		.message-container {
			display: flex;
			flex-direction: column;
			align-items: flex-start;
			max-width: 600px;
			margin: 0 auto;
			padding: 20px;
			background-color: #F4F4F4;
		}

		.message {
			display: inline-block;
			padding: 10px;
			border-radius: 10px;
			margin-bottom: 10px;
			max-width: 80%;
		}

		.sender {
			background-color: orange;
			color: white;
			align-self: flex-start;
		}

		.receiver {
			background-color: #FFFFFF;
			align-self: flex-end;
		}

		.text-input {
			display: block;
			width: 100%;
			padding: 10px;
			border: none;
			border-top: 1px solid #CCCCCC;
			box-sizing: border-box;
			resize: none;
			font-size: 16px;
			font-family: sans-serif;
			margin-bottom: 10px;
		}

		.send-button {
			display: block;
			background-color: orange;
			color: white;
			border: none;
			padding: 10px;
			font-size: 16px;
			border-radius: 5px;
			cursor: pointer;
		}

		.send-button:hover {
			background-color: #3E8E41;
		}
	
	</style>
</head>
<body>
	<div class="container-fluid">
  <div class="row">
    <div class="col-md-6">
      <div class="container mt-4 bg-warning">
        <h2 class="font-weight-bold mb-4">Users</h2>
        <ul class="list-group">
          <li class="list-group-item"><a href="#">User 1</a></li>
          <li class="list-group-item"><a href="#">User 2</a></li>
          <li class="list-group-item"><a href="#">User 3</a></li>
          <li class="list-group-item"><a href="#">User 4</a></li>
        </ul>
      </div>
    </div>
    <div class="col-md-6">
      <div class="message-container mt-4">
        <div class="message receiver">Hey, how's it going?</div>
        <div class="message receiver">Excited to present our project?</div>
      </div>
      <form>
        <textarea class="text-input" id="message-input" placeholder="Type your message here"></textarea>
        <button type="submit" class="send-button" onclick="sendMessage()">Send</button>
      </form>
    </div>
  </div>
</div>
	
	<script>
		function sendMessage() {
			event.preventDefault(); //Prevents page from refreshing, which would normally clear text box
			var message = document.getElementById("message-input").value;
			var messageContainer = document.querySelector(".message-container");
			var messageDiv = document.createElement("div");
			messageDiv.textContent = message;
			messageDiv.classList.add("message", "sender");
			messageContainer.appendChild(messageDiv);
		}
	</script>
</body>
</html>